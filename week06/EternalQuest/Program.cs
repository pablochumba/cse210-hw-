using System;

class Program
{
    static void Main(string[] args)
    {
        // Creativity: displays a rank title based on score to add a small gamification layer beyond the base requirements.
        GoalManager goalManager = new GoalManager();
        goalManager.Start();
    }
}
